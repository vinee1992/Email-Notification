package com.mckesson.integration.notification.config;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class dummy {
	boolean i;

	public static void main(String[] args) {
		if(i) {
		// TODO Auto-generated method stub

		/*
		 * List<Number> n = Arrays.asList(2, 3, 4, 5, 6); List<Number> n2 =
		 * Arrays.asList(9, 8, 7, 6);
		 * 
		 * List<Number> finalList = n.stream().filter(item ->
		 * !n2.contains(item)).collect(Collectors.toList());
		 * dummy
		 */
		
		dummy d = new dummy();
		System.err.println(d.i);
	}

}
